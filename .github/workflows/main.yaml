name: Angular Build and Deploy

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Acessando pasta do front
        run : ls -la
        
      # - name: Baixando dependencias
      #   run: npm install

      # - name: Construindo aplicação
      #   run: npm run build:prod